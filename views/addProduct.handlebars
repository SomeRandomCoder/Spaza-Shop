<div class= "row-list">
	<h1>Add Product</h1>
	<form action="/products/addProduct" method="POST">
	<div class="">
	<h4>Category</h4>
	<select class="browser-default" name="category_id">
		{{#categories}}
			<option value="{{id}}">{{category}}</options>
				{{/categories}}
			</select>
		</div>


	<div class="">
    <h4>Product</h4>
    <input type="text" name="product">
	</div>
<input
	<button type="submit" value="Add Product" class="btn btn-primary"></button>
</form>
</div>
